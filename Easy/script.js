function exercise(workout) {
    var challenge = `Today's exercise: ${workout}`;
    return challenge;
}

var run = exercise('running');
console.log(run);

var swim = exercise('swimming');
console.log(swim);